{
    "name": "artifactory-vsts-extension",
    "version": "1.0.0",
    "author": "JFrog",
    "license": "Apache-2.0",
    "dependencies": {
        "rimraf": "^2.6.2",
        "tfx-cli": "^0.5.14"
    },
    "scripts": {
        "preinstall": "npm i --prefix=buildScripts",
        "install": "node buildScripts/build.js",
        "release": "npm i && node buildScripts/release.js",
        "create": "npm i && tfx extension create",
        "test": "npm i && npm t --prefix=tests"
    }
}
